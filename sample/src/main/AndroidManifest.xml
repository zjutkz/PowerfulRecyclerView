<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kangzhe.sample">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.RecyclerListViewActivity"></activity>

        <activity android:name=".activity.RecyclerGridViewActivity"></activity>

        <activity android:name=".activity.RecyclerStaggeredViewActivity"></activity>

        <activity android:name=".activity.SimplePtrActivity"></activity>

        <activity android:name=".activity.HorizontalActivity"></activity>

    </application>


</manifest>
